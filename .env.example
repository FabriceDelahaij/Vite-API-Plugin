# Security Configuration
# IMPORTANT: Never commit this file to version control!
# Generate secure secrets using: node scripts/generate-secrets.js

# JWT Secret (minimum 32 characters, recommended 64+)
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-min-32-chars

# Session Secret (for session-based auth)
SESSION_SECRET=your-session-secret-here-change-this

# Cookie Secret (for signed cookies)
COOKIE_SECRET=your-cookie-secret-here-change-this

# API Keys (format: key:name:permissions)
API_KEY=your-api-key-here
API_TOKEN=your-api-token-here

# CORS Configuration
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Cookie Domain (optional, for subdomain support)
# Use .example.com to allow all subdomains
COOKIE_DOMAIN=

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Sentry Error Tracking
# Get your DSN from https://sentry.io
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# SSL Certificates (Production)
SSL_KEY_PATH=/etc/ssl/private/key.pem
SSL_CERT_PATH=/etc/ssl/certs/cert.pem

# Server Configuration
PORT=5173
HOST=localhost

# Environment
NODE_ENV=development

# Debug Mode
DEBUG=false

# HTTPS (set to true to enable HTTPS in development)
HTTPS=false
